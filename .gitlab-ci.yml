

pages:
  image: alpine:latest
  script:
  - apk add --no-cache mdbook pipx
  - pipx install --global linkchecker
  - cd info-débranchée ; mdbook build
  - mkdir ../public; mv book/* ../public
  - linkchecker ../public
  artifacts:
    paths:
    - public
  only:
  - main
